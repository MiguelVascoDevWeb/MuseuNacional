function direita() {
    const container = document.getElementById("carrossel");
    container.scrollBy(500, 0)
}

function esquerda() {
    const container = document.getElementById("carrossel");
    container.scrollBy(-500, 0);
}
